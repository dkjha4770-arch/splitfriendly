# Database URL (PostgreSQL / Neon connection string)
DATABASE_URL=

# JWT Sign secret key
JWT_SECRET=

# Node Environment
NODE_ENV=production

# Allowed origins for CORS (comma-separated URLs, e.g. https://your-app.vercel.app)
ALLOWED_ORIGINS=
